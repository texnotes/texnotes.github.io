var controls = true,
    progress = true,
    history = fase;		// Careful with variable name 'history'
 
var HORIZONTAL_SELECTOR; // Something must be as = '.reveal ''.slides>section.present>section';


 
var tax = 0.16;
var price = 100;
var total;
	
	total = price * 1 + tax; // Careful with calculations, maybe total = price * (1 + tax); ??  

var layout = '<div class="main"></div> \
    <div class="wrapper"> \
    <div class="sidebar"></div> \
    <div class="content"></div> \
	    </div>';							// Possible missed '</div>' in class="wrapper"

var MAX_INDEX = 999;					
var Result;
Result = 'Max index is ' + MAX_INDEX;

