Реализация класса TagList. Конструктор принимает первым аргументом узел в который будет помещен html виджета, 
и вторым опциональным параметром список предопределенных тегов.

Виджет имеет 2 режима отображения: показ и редактирование. 
В режиме редактирования можно добавлять и удалять теги. Добавлять уже существующие теги нельзя.

Добавление происходит по enter и по клику на кнопку "добавить". 
Реализован метод getTagList, возвращающий список тегов (массив строк).
----------------------------------------------------------------------

Пример инициализации:
var el = makeTagList(document.querySelector('.widget'),['Первый', 'Второй', 'Третий', 'Последний']);
Вызов функции, возвращающей теги: el.getTagList();

----------------------------------------------------------------------
В отличие от задания класс спрятан в анонимную функцию и для инициализации не нужен "New",
думаю, что так лучше, но всегда можно исправить - убрать анонимную функцию.

Не сделал валитдацию ввода тегов, поскольку не было в задании, но было бы неплохо.
Однако, еще лучше было бы, если пользователь мог подключить свой модуль валитдации,
но пока знаний маловато для реализации таких кросс-виджитов.

Минус текущей версии - плохая разметка, учусь но пока с трудом дается верстка.
Соответственно, плохая верстка потянула сильную на нее завязку кода, что может
сказаться на его стабильности при расширении плагина.

К сожалению, мало время осталось на тестирование. Не исключаю досадные :-) ошибки.

Немного разобрался с docco - отличная система! В папке \js\docs результат моей документации. 

