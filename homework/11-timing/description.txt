Виджет "секундомер". 
Кнопки "стоп", "старт" (одна и та же кнопка), "сброс", "круг". 

Секундомер работает с точностью до миллисекунд. 
Когда он запущен, показывается время в формате чч:мм:сс:мс. 
Нули разрядов не показываются. 

Нажатие на кнопку "стоп" останавливает ход секундомера. 
Кнопка "сброс" останавливает секнудомер, если тот бежит, сбрасывает значение в нули, убирает все результаты, если такие есть.

Кнопка "круг" добавляет текущее значение секундомера в список результатов. 
Элементы из списка результатов можно удалять поштучно. 
 
ВАЖНО: Управление с клавиатуры не реализовано. 

Пояснение: не успел реализовать поддержку клавиатуры (Банально, но не хватило времени). Реализовывал бы следующим образом:
-реагировал бы на скан коды клавиатуры, а не символы, чтобы не зависить от раскладок. Обработчики событий готовы, 
но есть вопрос, как узнать в какой момент нужно определенному таймеру реагировать на клавиатуру.
Решал бы так - ввел переменную-флажок, что событие вызвано по клику клавиатуры, а нужно ли обрабатывать
этот вызов зависело бы от проверки ссылки на элемент таймера, над последним из которым была мышка.
Тут помогло бы делегирование, например во всем контейнере можно было смотреть за мышкой, если над зоной 
любого таймера - сбрасывать ссылку на его место в DOM для переменной, которую увидит функция Start/Lap и т.д.

Проблема с кроссбраузерностью особенно проявилась для удаления элементов из списка результатов.
Не поддерживается IE. Причина - не умею правильно передавать ссылку на event.target. Для реализации 
не хватило времени разобраться с кроссбраузерностью функций addEvent и bind.

ВАЖНО: не хватило времени привести в порядок функцию отображения результата, чтобы не учитывать нули разрядов.
А так же сброс таймера должен приводить к записи "00:00:00:000" или "0"? Так же обнаружил путаницу
с отображением количества единиц в милисекундах две или три. У меня четыре, получить меньшее 
кол-во можно делением: var milliseconds = parseInt(duration%1000/10); 

C незначащими нулями должно быть так (c учетом старших разрядов):

if (hours===0) {
	hours=''; 
		if (minutes===0) { minutes ='';
			if(seconds===0) seconds ='';
		}
	}

Приведенный код в readme.txt в проект не вносил, 
жаль, но уже у меня нет достаточного времени на его тестирование в составе проекта.   
(Хочу успеть отформатировать код и дать комментарии)
